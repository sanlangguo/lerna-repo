{
  "name": "root",
  "private": true,
  "scripts": {
    "clean": "lerna clean",
    "bootstrap": "lerna bootstrap --hoist",
    "start:a": "lerna --scope @mono-repo/a run start",
    "start:b": "lerna --scope @mono-repo/b run start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.3.0",
    "less": "^4.1.3",
    "less-loader": "^11.0.0"
  },
  "dependencies": {
    "classnames": "^2.3.1"
  }
}
